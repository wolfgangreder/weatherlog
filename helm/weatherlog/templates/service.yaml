apiVersion: v1
kind: Service
metadata:
  name: {{ include "weatherlog.fullname" . }}
  labels:
    {{- include "weatherlog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "weatherlog.selectorLabels" . | nindent 4 }}
