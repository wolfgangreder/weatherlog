{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "weatherlog.serviceAccountName" . }}
  labels:
    {{- include "weatherlog.labels" . | nindent 4 }}
{{- end }}
